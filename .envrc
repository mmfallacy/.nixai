dotenv

export NIXAI_AGENTFORMS_ROOT=$(pwd)/agentforms

if has nix; then
  nix_direnv_manual_reload

  files=$(git ls-files --cached --others --exclude-standard -- nix/)
  for file in files; do
    watch_file "$file"
    echo "direnv: watching $file"
  done

  use flake
fi
